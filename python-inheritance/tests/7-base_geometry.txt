The BaseGeometry class
======================

Importing the class:
    >>> BaseGeometry = __import__('7-base_geometry').BaseGeometry
    >>> bg = BaseGeometry()

Checking area():
    >>> try:
    ...     bg.area()
    ... except Exception as e:
    ...     print(e)
    area() is not implemented

Checking integer_validator() with valid input:
    >>> bg.integer_validator("age", 20)
    >>> bg.integer_validator("width", 89)

Checking integer_validator() with non-integer (string):
    >>> try:
    ...     bg.integer_validator("age", "30")
    ... except TypeError as e:
    ...     print(e)
    age must be an integer

Checking integer_validator() with 0:
    >>> try:
    ...     bg.integer_validator("age", 0)
    ... except ValueError as e:
    ...     print(e)
    age must be greater than 0

Checking integer_validator() with negative number:
    >>> try:
    ...     bg.integer_validator("age", -5)
    ... except ValueError as e:
    ...     print(e)
    age must be greater than 0

Checking integer_validator() with float:
    >>> try:
    ...     bg.integer_validator("age", 15.5)
    ... except TypeError as e:
    ...     print(e)
    age must be an integer

Checking integer_validator() with list:
    >>> try:
    ...     bg.integer_validator("age", [1, 2])
    ... except TypeError as e:
    ...     print(e)
    age must be an integer

Checking integer_validator() with tuple:
    >>> try:
    ...     bg.integer_validator("age", (1,))
    ... except TypeError as e:
    ...     print(e)
    age must be an integer

Checking integer_validator() with True (boolean):
    >>> try:
    ...     bg.integer_validator("age", True)
    ... except TypeError as e:
    ...     print(e)
    age must be an integer

Checking integer_validator() with set:
    >>> try:
    ...     bg.integer_validator("age", {3, 4})
    ... except TypeError as e:
    ...     print(e)
    age must be an integer

Checking integer_validator() with None:
    >>> try:
    ...     bg.integer_validator("age", None)
    ... except TypeError as e:
    ...     print(e)
    age must be an integer
